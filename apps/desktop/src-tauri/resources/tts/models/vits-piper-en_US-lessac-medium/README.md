# Starter Model Placeholder

Expected files for the starter model:

- `en_US-lessac-medium.onnx`
- `tokens.txt`
- `espeak-ng-data/`

The model registry in `../models.json` points to these relative paths.

