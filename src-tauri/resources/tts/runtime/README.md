# Runtime Placeholder

Place the Windows sherpa-onnx runtime executable here before packaging:

`sherpa-onnx-offline-tts.exe`

The Rust command layer invokes this executable directly from the Tauri resource directory.

